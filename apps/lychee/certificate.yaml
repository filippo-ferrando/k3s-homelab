apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lychee-internal-tls
  namespace: lychee
spec:
  secretName: lychee-internal-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - photo.internal.fferrando.cc
