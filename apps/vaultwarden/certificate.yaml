apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vaultwarden-internal-tls
  namespace: vaultwarden
spec:
  secretName: vaultwarden-internal-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - vault.internal.fferrando.cc
