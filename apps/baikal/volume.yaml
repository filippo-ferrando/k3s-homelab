apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: baikal-data
  namespace: baikal
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
