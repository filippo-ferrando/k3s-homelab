apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forgejo-external-tls
  namespace: forgejo
spec:
  secretName: forgejo-external-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - forgejo.fferrando.cc
