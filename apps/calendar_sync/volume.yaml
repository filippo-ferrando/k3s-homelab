apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vdirsyncer-status
  namespace: baikal
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
