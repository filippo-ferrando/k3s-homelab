apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-internal-tls
  namespace: traefik
spec:
  secretName: traefik-internal-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - traefik.internal.fferrando.cc
