apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-ui
  namespace: monitoring
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`stats.internal.fferrando.cc`)
      kind: Rule
      services:
        - name: kube-prometheus-stack-grafana
          port: 80
