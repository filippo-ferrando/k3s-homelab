ingress:
  enabled: false

defaultSettings:
  defaultReplicaCount: 3
  backupbopsCleanupWaitInterval: 1440
  nodeDrainPolicy: "allow-if-replica-is-stopped"

preUpgradeChecker:
  jobEnabled: false

metrics:
  serviceMonitor:
    enabled: true
