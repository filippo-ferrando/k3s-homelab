ingress:
  enabled: false

defaultSettings:
  defaultReplicaCount: 3
  backupbopsCleanupWaitInterval: 1440
  nodeDrainPolicy: "always-allow"

preUpgradeChecker:
  jobEnabled: false

metrics:
  serviceMonitor:
    enabled: true
